import React from "react";

const Title = ({ title }) => {
  return (
    <div className="mb-[10px] xs:mb-[20px] mx-0 bxs:text-[40px] xs:text-[34px] text-[24px] font-serif">
      {" "}
      {title}
    </div>
  );
};

export default Title;
